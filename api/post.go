package api

import (
  "net/http"
)

// createPost adds a new post to the database
func (s *Server) createPost(w http.ResponseWriter, r *http.Request) {
}

// getPostByID fetches a post from the database using the id
func (s *Server) getPostByID(w http.ResponseWriter, r *http.Request) {
}

// getUserPosts fetches a post from the database for a particular user
func (s *Server) getUserPosts(w http.ResponseWriter, r *http.Request) {
}
